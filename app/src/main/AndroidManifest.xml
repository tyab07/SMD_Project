<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:usesCleartextTraffic="true"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.FastConnect">

        <!-- Splash Screen / Entry Point -->
        <activity
            android:name=".activities.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        
        <activity android:name=".activities.RoleSelectionActivity" />
        
        <!-- Feature Activities -->
        <activity android:name=".activities.SignInActivity" />

        <!-- Sign Up Screen -->
        <activity android:name=".activities.SignUpActivity" />

        <!-- Dashboard - Main Fragment Container -->
        <activity android:name=".activities.DashboardActivity" />

        <!-- Admin Flows -->
        <activity android:name=".activities.AdminDashboardActivity" />
        <activity android:name=".activities.AddSocietyActivity" />
        <activity android:name=".activities.AddAnnouncementActivity" />

        <activity
            android:name=".activities.NotificationInboxActivity"
            android:exported="false"
            android:label="Notifications"
            android:theme="@style/Theme.AppCompat.NoActionBar" />

        <service
            android:name=".firebase.FastConnectMessagingService"
            android:exported="false">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>

        <meta-data
            android:name="com.google.firebase.messaging.default_notification_channel_id"
            android:value="fast_connect_channel" />
    </application>

</manifest>